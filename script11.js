alert("Area Losango");
let dmaior = parseFloat (prompt ("Digite a diagonal maior: "));
let dmenor = parseFloat (prompt ("Digite a diagonal menor: "));
let area = dmaior * dmenor / 2;
alert("O valor da area é: "+ area +".")